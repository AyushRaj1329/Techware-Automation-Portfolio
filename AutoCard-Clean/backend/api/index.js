// Vercel serverless entry point.
// Imports the Express app and exports it as the default handler
// so Vercel can invoke it as a serverless function.
import app from "../src/index.js";

export default app;
